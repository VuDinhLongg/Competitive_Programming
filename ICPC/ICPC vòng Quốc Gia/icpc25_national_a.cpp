/*======================
   Author : @LonggVuz
======================*/
#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include <debug.h>
#else
#define dbg(...) 1608
#define orz(...) 2005
#endif

#define el cout << '\n'
#define all(x) begin(x), end(x)
#define fix(x) fixed << setprecision(x)
#define out(x) return void(cout << (x) << '\n')
#define rep(i, n) for(int32_t i=0; i<n; ++i)
#define fo(i, a, b) for(int32_t i=a; i<=b; ++i)
#define fd(i, a, b) for(int32_t i=a; i>=b; --i)
#define maxi(a, b) ((a) < (b) ? (a = b, 1) : 0)
#define mini(a, b) ((a) > (b) ? (a = b, 1) : 0)
#define int long long
#define mxn 1'000'007



inline void LonggVuz(){
    int i; cin >> i;
    if(i == 1) out("Uphold integrity and ethics throughout the contest.");
    if(i == 2) out("Do not seek or receive external help from people, platforms, tools or AI.");
    if(i == 3) out("Follow all ICPC rules and guidelines, accept decisions made by organizers and judges as final.");
    if(i == 4) out("Show good sportsmanship and treat competitors, volunteers, staff and judges with respect.");
    out("Compete with creativity and teamwork, honor the contest spirit and pursue excellence.");
}

signed main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);
    
    signed o = 1; if(false) cin >> o;
    rep(i, o) orz(i), LonggVuz();
    
    cerr << "[exec time = `" << clock() << "ms`]";
}